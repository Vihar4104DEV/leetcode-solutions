# Submission timestamp: 2026-05-09T05:52:21.434Z

1516141713121110987654321            else:                d[i] = 1                    ans.append(i)                    if d[i] == k:                d[i] += 1            if i in d:                return ans            if len(ans) >= k:        for i in nums:            return [nums[0]]·‌·‌·‌·‌·‌·‌·‌·‌if k <=1 and len(nums) == 1:·‌·‌·‌·‌·‌·‌·‌·‌d·‌=·‌{}·‌·‌·‌·‌·‌·‌·‌·‌ans·‌=·‌[]    def·‌topKFrequent(self,·‌nums:·‌List[int],·‌k:·‌int)·‌->·‌List[int]:class Solution:
else:                d[i] = 1                    ans.append(i)                    if d[i] == k:                d[i] += 1            if i in d:                return ans            if len(ans) >= k:        for i in nums:            return [nums[0]]·‌·‌·‌·‌·‌·‌·‌·‌if k <=1 and len(nums) == 1:·‌·‌·‌·‌·‌·‌·‌·‌d·‌=·‌{}·‌·‌·‌·‌·‌·‌·‌·‌ans·‌=·‌[]    def·‌topKFrequent(self,·‌nums:·‌List[int],·‌k:·‌int)·‌->·‌List[int]:class Solution:
else:
d[i] = 1
ans.append(i)
if d[i] == k:
d[i] += 1
if i in d:
return ans
if len(ans) >= k:
for i in nums:
return [nums[0]]
·‌·‌·‌·‌·‌·‌·‌·‌if k <=1 and len(nums) == 1:
·‌·‌·‌·‌·‌·‌·‌·‌d·‌=·‌{}
·‌·‌·‌·‌·‌·‌·‌·‌ans·‌=·‌[]
def·‌topKFrequent(self,·‌nums:·‌List[int],·‌k:·‌int)·‌->·‌List[int]:
class Solution:
